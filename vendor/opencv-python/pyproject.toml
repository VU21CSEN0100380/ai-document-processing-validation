[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "opencv-python"
version = "5.0.0.93"
description = "Dependency shim that substitutes headless OpenCV for server deployments."
requires-python = ">=3.8"
dependencies = ["opencv-python-headless==5.0.0.93"]

[tool.setuptools]
packages = []
